app = 'demouser-crm'
primary_region = 'sjc'

[build]

[env]
  PORT = '8080'
  HOST = '0.0.0.0'
  DATA_DIR = '/app/pipeline/output-demo'

[http_service]
  internal_port = 8080
  force_https = true
  auto_stop_machines = 'stop'
  auto_start_machines = true
  min_machines_running = 0
